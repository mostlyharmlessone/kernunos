include ../../Makefile.inc

# Use vpath patterns to pick up source files from
# other chapters, e.g., set_precision, etc
# Could move this into Makefile.inc so one change would
# fix directory name changes.
vpath %.f90 ../Chapter02 

executables = approximatePi matvecmult

all: $(executables)

approximatePi: set_precision.o approximatePi.o

matvecmult: set_precision.o matvecmult.o

approximatePi.o: set_precision.o

matvecmult.o: set_precision.o


.PHONY: clean
clean: 
	# remove all files generated by the compiler
	# these are compiler dependent
	rm -f *.o *.d *.g90 *.mod
	# remove all executables
	rm -f $(executables)
