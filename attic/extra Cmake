    # add dependencies
    # download CPM.cmake, can't count on installed libraries
#    file(
#      DOWNLOAD
#      https://github.com/cpm-cmake/CPM.cmake/releases/download/v0.40.8/CPM.cmake
#      ${CMAKE_CURRENT_BINARY_DIR}/cmake/CPM.cmake
#      EXPECTED_HASH SHA256=78ba32abdf798bc616bab7c73aac32a17bbd7b06ad9e26a6add69de8f3ae4791
#    )
#    include(${CMAKE_CURRENT_BINARY_DIR}/cmake/CPM.cmake)
#    set(CPM_USE_LOCAL_PACKAGES ON)
    set(BLA_VENDOR Generic)
#    find_package(BLAS REQUIRED)
#    if (BLAS_FOUND )
#     message(STATUS "BLAS found at ${BLAS_LIBRARIES}")
#    endif()
#    find_package(LAPACK REQUIRED)
#    if (LAPACK_FOUND)
#     message(STATUS "LAPACK found at ${LAPACK_LIBRARIES}")
#    else()
#      CPMAddPackage(
#              NAME LAPACK
#              VERSION 3.12.1
#              URL http://www.netlib.org/lapack/lapack-3.12.1.tar.gz
#              OPTIONS
#                  "BUILD_TESTING OFF"
#                  "LAPACKE ON" # Enable C interface if needed
#      )
#    CPMAddPackage(
#        NAME LAPACK
#        GIT_TAG v3.12.0 # Use the latest stable tag
#        GITHUB_REPOSITORY Reference-LAPACK/lapack
#        OPTIONS
#            "BUILD_TESTING OFF"
#            "LAPACKE ON" # Enable C interface if needed
#    )
      # we need to set proper include headers
#      if(LAPACK_ADDED)
        # Set include directories for the targets
#        target_include_directories(blas INTERFACE "${LAPACK_SOURCE_DIR}/INCLUDE")
#        target_include_directories(lapack INTERFACE "${LAPACK_SOURCE_DIR}/INCLUDE")
#        set(BLAS_LIBRARIES blas)
#        set(LAPACK_LIBRARIES lapack)
#      endif()